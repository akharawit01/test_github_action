name: Greet Everyone

on: [push]
jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - run: echo This event is runing my_first_job.
      - uses: actions/setup-node@v1

      - name: yarn install
        run: yarn install

  my_second_job:
    name: My second job
    needs: my_first_job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1

      - name: yarn test
        run: yarn test

      - name: yarn build
        run: yarn build
